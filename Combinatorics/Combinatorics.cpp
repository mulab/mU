#include <mU/Combinatorics.h>
#ifdef _MSC_VER
#pragma comment(lib,"Kernel.lib")
#endif

using namespace mU;

CAPI void Install(Kernel& k) {
	//wcout << "#Combinatorics# Install Successfully!" << endl;
}
CAPI void Uninstall(Kernel& k) {
	wcout << "#Combinatorics# Uninstall Successfully!" << endl;
}